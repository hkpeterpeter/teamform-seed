'use strict';
/*
describe('search module', funcion(){
	
	describe('searchCtrl', function() {
		beforeEach(module('search', 'firebase'));
		
		it('should find the team with the keywords in the phase', 
		inject(function($controller, $firebase){
			var scope = {};
			var ctrl = $controller('searchCtrl', {$scope: scope});
			var firebase = $firebase(new Firebase('url'));
			
			var searchText = "dummy group"
			scope.search.keywords = searchText.split(" ");
			scope.search();
			
			// Assume we have team named dummy test group
			expect(scope.result).toEqual(dummy test group);
			
		})
	})

});
*/